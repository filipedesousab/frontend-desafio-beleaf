// import {} from './types';

const INITIAL_STATE = {
  user: {
    id: '1',
  },
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
